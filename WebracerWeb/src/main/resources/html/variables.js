// some global variables, editable

// time per round in milliseconds
var TIMEOUT = 1000;

// speed in px / TIMEOUT
var SPEED1 = 15;
var SPEED2 = 40;

// steering left/right
var DEGREES = 20;

// nr of laps to go
var NR_LAPS = 3;
